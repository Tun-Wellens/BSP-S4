# Core dependencies
torch>=2.0.0
transformers>=4.38.0
datasets>=2.18.0
evaluate>=0.4.0
soundfile>=0.12.1
accelerate>=0.25.0

# Whisper-specific
git+https://github.com/openai/whisper.git

# For audio processing (used by `datasets.Audio`)
librosa>=0.10.0

# Weights & Biases for logging
wandb>=0.16.0

# ensure compatible with your PyTorch version
torchvision==<match-your-pytorch-version>
torchaudio==<match-your-pytorch-version>
